// API Calls for authentication module.

export default {
  user: {
    login: credentials => {
       console.log("Simulating axios call to backend");
      // axios.post("/api/auth", { credentials }).then(res => res.data.user)
    }
  }
};